# Frontend (Vite) — set in .env.local for local dev, or in Vercel env vars
VITE_SERVER_URL=http://localhost:3001

# Backend (server/) — set in Railway env vars for production
PORT=3001
ADMIN_TOKEN=change-me-to-a-secret-token
CORS_ORIGIN=http://localhost:5173
